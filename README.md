# Medical Insurance Cost Prediction 🏥💊

This project aims to predict the **medical insurance charges** for customers based on several factors like age, BMI, smoking status, and region using **Linear Regression**.

## 🚀 Project Workflow
* **Exploratory Data Analysis (EDA):** Visualizing data distribution and correlations.
* **Data Preprocessing:** Handling categorical variables using **One-Hot Encoding**.
* **Model Building:** Training a **Linear Regression** model.
* **Evaluation:** Measuring performance using the **R-squared (R²)** score.

## 🛠️ Technologies Used
* **Python**
* **Pandas & NumPy** (Data Handling)
* **Seaborn & Matplotlib** (Visualization)
* **Scikit-learn** (Machine Learning Model)

## 📈 Model Performance
* The model achieved an **R-squared score of approximately 80.7%**, indicating a strong correlation between the features and the predicted charges.

## 📁 Key Steps
1.  **Libraries Import:** Loading data processing and ML tools.
2.  **Visualization:** Analyzing `bmi`, `age`, and `smoker` status.
3.  **Encoding:** Converting text data (like 'sex', 'region') into numbers.
4.  **Training:** Splitting data into `train` and `test` sets.
5.  **Testing:** Comparing actual vs. predicted charges.
